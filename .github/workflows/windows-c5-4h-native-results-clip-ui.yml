name: Windows C5.4h Native Results Clip UI

on:
  pull_request:
  workflow_dispatch:

jobs:
  native-results-clip-ui:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install
        run: python -m pip install -e ".[test]"
      - name: Compile warnings as errors
        run: python -W error -m py_compile src/astermax/fea/results_workspace_ui.py src/astermax/fea/results_clip.py
      - name: C5.4h native clip UI harness
        run: python -m pytest -q tests/test_results_workspace_ui.py tests/test_results_clip.py tests/test_results_workspace.py tests/test_production_results.py
      - name: Full regression suite
        run: python -m pytest -q
