name: Windows C5.4c Production Results Provenance

on:
  pull_request:
  workflow_dispatch:

jobs:
  production-results-provenance:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install
        run: python -m pip install -e ".[test]"
      - name: Compile warnings as errors
        run: python -W error -m py_compile src/astermax/fea/production_results.py src/astermax/fea/results_workspace.py src/astermax/fea/results_workspace_ui.py src/astermax/desktop_picker_app.py
      - name: C5.4c production results provenance harness
        run: python -m pytest -q tests/test_production_results.py tests/test_results_workspace_ui.py tests/test_results_workspace.py tests/test_desktop_picker_app.py
      - name: Full regression suite
        run: python -m pytest -q
